<%= form_for(@step_status) do |f| %>
  <% if @step_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@step_status.errors.count, "error") %> prohibited this step_status from being saved:</h2>

      <ul>
      <% @step_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="row">
      <div class="span5 offset3">
          <div class="field">
            <%= f.label :stepstatus_name %><br>
            <%= f.text_field :stepstatus_name %>
          </div>
          <div class="actions">
            <%= f.submit %>
          </div>
      </div>
    </div>

<% end %>
