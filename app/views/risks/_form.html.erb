<%= form_for(@risk) do |f| %>
  <% if @risk.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@risk.errors.count, "error") %> prohibited this risk from being saved:</h2>

      <ul>
      <% @risk.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="container">

      <div class="field">
        <%= f.label :risk_name %><br>
        <%= f.text_field :risk_name %>
      </div>
      <div class="actions">
        <%= f.submit %>
      </div>

  </div>
<% end %>
